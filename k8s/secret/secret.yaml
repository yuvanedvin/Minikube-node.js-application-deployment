# ─────────────────────────────────────────────────────────────────────────────
# IMPORTANT: Never commit real secret values to git.
# Use: echo -n 'your-value' | base64  to encode values.
# In production, use Sealed Secrets or Vault instead of raw Secrets.
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: monitoring-app-secret
  namespace: monitoring-app
type: Opaque
data:
  # base64 encoded values — replace with real ones
  # echo -n 'your-api-key' | base64
  API_KEY: eW91ci1hcGkta2V5      # placeholder: "your-api-key"
  DB_PASSWORD: Y2hhbmdlbWU=       # placeholder: "changeme"
